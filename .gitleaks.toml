title = "GuGuData knowledge base"

[extend]
useDefault = true

[allowlist]
description = "Documented placeholder credentials"
regexTarget = "match"
regexes = [
  '''YOUR_(?:TOKEN|VALUE|APP_?KEY)''',
  '''REDACTED'''
]
